{
  "name": "loopy",
  "version": "0.1.0",
  "description": "A path-drawing puzzle",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "record:tutorial": "node scripts/record-tutorial.mjs",
    "boards:tutorial": "node scripts/tutorial-boards.mjs",
    "build": "npm run check:i18n && node scripts/build-locales.mjs",
    "build:single": "vite build",
    "build:itch": "BUILD_TARGET=itch vite build --outDir dist-itch && cd dist-itch && zip -r ../loopy-itch.zip . -x '*.DS_Store' -x '_redirects'",
    "check:i18n": "node scripts/check-i18n.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "ffmpeg-static": "^5.3.0",
    "playwright": "^1.62.1",
    "vite": "^5.4.0"
  },
  "dependencies": {
    "@fontsource/inter": "^5.2.8",
    "@fontsource/monoton": "^5.2.7",
    "lucide": "^0.555.0",
    "posthog-js": "^1.409.5"
  }
}
